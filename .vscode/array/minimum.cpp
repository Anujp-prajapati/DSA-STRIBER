// Find the maximum and minimum in an array using only one traversal
#include <bits/stdc++.h>
using namespace std;
int max_min(int a, int b, int n)
{
    a = 0;
    b = 0;
    int arr[n];
    cout << "enter the size of an array";
    cin >> n;

    cout << "enter the element in an array ";
    cin >> arr[n];

    for (int i = 0; i < n; i++)
    {
        
        
    }
}

int main()
{
}
